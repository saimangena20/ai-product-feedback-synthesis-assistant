# SQLite is the default local provider. Set a MySQL URL only when using MySQL.
DATABASE_URL=sqlite:///./feedback.db
# Required only for POST /api/v1/ingests/{ingest_id}/analysis.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
